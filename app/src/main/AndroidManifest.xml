<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.uk.outlook.davidslambert.warcompanion">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".presention.game.GameActivity"></activity>
        <activity android:name=".presention.gameselectorbase.resumegameselector.ResumeGameSelectorActivity" />
        <activity android:name=".presention.gameselectorbase.completegameselector.CompleteGameSelectorActivity" />
        <activity
            android:name=".presention.setup.ui.SetUpActivity"
            android:noHistory="true"
            android:label="@string/title_activity_set_up">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity android:name=".presention.startscreen.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>